struct Account {
    var id: Int
    var sum: Int // 
    var userId: Int
    
    init(id: Int, sum: Int, userId: Int) {
        self.id = id
        self.sum = sum
        self.userId = userId
    }
}
